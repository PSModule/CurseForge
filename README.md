# CurseForge

CurseForge is intended to be a PowerShell module for interacting with the CurseForge API.

## Status

This repository is currently a placeholder. The module source and `examples/` directory still contain scaffold code, so there are no supported commands or module-specific usage examples to document yet.

## Documentation

When this module is implemented, command details should live in PowerShell help and generated documentation rather than being duplicated in this README.
